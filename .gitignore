# Rust build output
/target

# Cargo install registry (only present if cargo was previously configured to
# install into the project root via `.cargo/config.toml`; no longer generated)
/.crates.toml
/.crates2.json


# Compiled llama.cpp binaries
/bin

# Downloaded models
/models

# Claude Code local settings
/.claude/settings.local.json

# Environment / secrets
.env
.env.*
!.env.example

# Editor
.vscode/
.idea/
*.swp
*.swo

# User defined configuration file(s)
shio.toml.*
